import { prisma } from '@/lib/prisma';

export default async function SjphMasterPage() {
  const categories = await prisma.sjphCategory.findMany({ include: { _count: { select: { criteria: true } } }, orderBy: { sortOrder: 'asc' } });
  return <div className="space-y-6"><div><p className="mb-2 text-xs font-bold text-[#08725b]">Administrasi LPH UNEJ</p><h1 className="display-font text-3xl font-extrabold tracking-[-.05em] text-[#10211e]">Master SJPH</h1><p className="mt-1 text-sm text-[#71847f]">Kategori dan jumlah kriteria pemeriksaan SJPH.</p></div><section className="grid gap-4 md:grid-cols-2">{categories.map(category => <div key={category.id} className="rounded-2xl border border-[#e2eeeb] bg-white p-5"><div className="flex items-center justify-between"><div><p className="text-[10px] font-bold text-[#08725b]">{category.code}</p><h2 className="mt-1 text-sm font-bold text-[#234940]">{category.name}</h2></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${category.active ? 'bg-[#e7f5ef] text-[#08725b]' : 'bg-[#f1f4f3] text-[#71847f]'}`}>{category.active ? 'Aktif' : 'Nonaktif'}</span></div><p className="mt-4 text-xs text-[#71847f]">{category._count.criteria} kriteria pemeriksaan</p></div>)}</section>{categories.length === 0 && <div className="rounded-2xl border border-dashed border-[#cfe2dc] bg-white p-12 text-center text-sm text-[#71847f]">Belum ada master SJPH.</div>}</div>;
}
