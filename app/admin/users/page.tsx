import { prisma } from '@/lib/prisma';

export default async function UsersPage() {
  const users = await prisma.user.findMany({ select: { id: true, name: true, email: true, role: true, company: true, createdAt: true }, orderBy: { name: 'asc' } });
  return <div className="space-y-6"><div><p className="mb-2 text-xs font-bold text-[#08725b]">Administrasi LPH UNEJ</p><h1 className="display-font text-3xl font-extrabold tracking-[-.05em] text-[#10211e]">Manajemen Pengguna</h1><p className="mt-1 text-sm text-[#71847f]">Daftar akun dan peran pengguna sistem.</p></div><section className="overflow-hidden rounded-2xl border border-[#e2eeeb] bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-xs"><thead className="bg-[#fbfdfc] text-[10px] font-bold text-[#8a9b97]"><tr><th className="px-5 py-3">Nama</th><th className="px-5 py-3">Email</th><th className="px-5 py-3">Peran</th><th className="px-5 py-3">Instansi</th><th className="px-5 py-3">Terdaftar</th></tr></thead><tbody>{users.map(user => <tr key={user.id} className="border-t border-[#edf3f1] text-[#526b66]"><td className="px-5 py-4 font-semibold text-[#234940]">{user.name}</td><td className="px-5 py-4">{user.email}</td><td className="px-5 py-4"><span className="rounded-full bg-[#e7f5ef] px-2.5 py-1 text-[10px] font-bold text-[#08725b]">{user.role}</span></td><td className="px-5 py-4">{user.company || '-'}</td><td className="px-5 py-4">{new Intl.DateTimeFormat('id-ID', { dateStyle: 'medium' }).format(user.createdAt)}</td></tr>)}</tbody></table></div></section></div>;
}
